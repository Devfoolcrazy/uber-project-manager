[package]
name = "uberpm"
version = "0.1.0"
description = "CLI and MCP server for Uber Project Manager"
edition = "2021"

[dependencies]
uberpm-core = { path = "../core" }
serde = "1"
serde_json = "1"
clap = { version = "4", features = ["derive"] }
